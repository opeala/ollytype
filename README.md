
  # Typing Test Application

  This is a code bundle for Typing Test Application. The original project is available at https://www.figma.com/design/j4rZUQaSZ1a2zI2Qv91ch3/Typing-Test-Application.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
  Run `npm run deploy` to trigger the deployment script to build the project and create/update the gh-pages branch automatically: